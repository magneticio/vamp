name: nomadic-frostbite

clusters:
  notorious:
    routing:
      web:
        sticky: route

    services:
      breed:
        name: nocturnal-viper
        deployable: anaconda
        ports:
          web: 8080/tcp
