name: nomadic-frostbite

clusters:
  notorious:
    routing:
      sticky: service

    services:
      breed:
        name: nocturnal-viper
        deployable: anaconda
        ports:
          web: 8080
          admin: 9090
