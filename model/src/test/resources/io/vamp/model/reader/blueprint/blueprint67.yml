name: nomadic-frostbite

clusters:
  notorious:
    gateways:
      sticky: route

    services:
      breed:
        name: nocturnal-viper
        deployable: anaconda
        ports:
          web: 8080
          admin: 9090
