name: nomadic-frostbite

clusters:
  notorious:
    routing:
      sticky: route

    services:
      breed:
        name: nocturnal-viper
        deployable: anaconda
        ports:
          web: 8080
          admin: 9090
