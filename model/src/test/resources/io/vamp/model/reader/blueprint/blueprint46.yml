name: nomadic-frostbite

endpoints:
  supersonic.port: 8080

clusters:
  supersonic:
    -
      breed: wordpress1
      scale: large
      route:
        filters:
          condition: "user.agent == android"