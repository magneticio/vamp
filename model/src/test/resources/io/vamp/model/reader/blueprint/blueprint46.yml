name: nomadic-frostbite

endpoints:
  supersonic.port: 8080

clusters:
  supersonic:
    breed: sava1
    scale: large
    routing:
      routes:
        sava1:
          filters:
            condition: "user.agent == android"
