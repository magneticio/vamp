name: nomadic-frostbite

parameters:
  supersonic.port: 8080

clusters:
  supersonic:
    breed:
      name: solid-barbershop
      deployable: vamp/solid-barbershop

      ports:
        name: port
        direction: IN