name: nomadic-frostbite

endpoints:
  supersonic.ports.port: $PORT

clusters:
  supersonic:
    breed:
      name: monarch
      deployable: magneticio/monarch:latest
      dependencies:
        db:
          name: monarch

