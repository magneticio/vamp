name: nomadic-frostbite

endpoints:
  omega.ports.port: $PORT

clusters:
  supersonic:
    breed:
      name: solid-barbershop
      deployable: vamp/solid-barbershop

      ports:
        name: port
        value: 80/http
        direction: OUT