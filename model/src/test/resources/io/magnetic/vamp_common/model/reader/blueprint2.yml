name: nomadic-frostbite

endpoints:
  notorious.ports.port: $PORT

clusters:
  notorious:
    services:
      -
        breed:
          name: nocturnal-viper
          
parameters:
  notorious.aspect: thorium