name: nomadic-frostbite

parameters:
  supersonic.port: $PORT

clusters:
  supersonic:
    breed:
      name: solid-barbershop
      deployable: vamp/solid-barbershop

      ports:
        name: port
        direction: IN